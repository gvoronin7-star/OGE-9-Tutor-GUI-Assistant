# ONBOARDING_ROADMAP — OGE-9-Tutor-GUI-Assistant

> Живой файл, часть паттерна «два файла вместо одного»
> (`01_vibe-coding-agent/scenarios/two-track-development.md`), только
> экспортированного сюда, в целевой проект. Обновляется сессиями,
> работающими внутри этого репозитория — не агентом-аудитором. Источник
> текущего состояния — `01_vibe-coding-agent/reports/
> 2026-08-16_OGE-9-Tutor-GUI-Assistant_fix-plan.md` (внешний аудит +
> план исправлений). Рядом — пустая папка `decisions/`: append-only
> журнал решений по этому проекту, тот же формат, что в
> `01_vibe-coding-agent`.

## Текущее состояние

По плану исправлений от 2026-08-16 (HEAD аудита `b62f989`):

### Технические правки

- [ ] Устаревшая версия в `.env.example:13` (`APP_VERSION=2.2.0` →
  `2.3`).
- [ ] Плейсхолдер `your-username` в `README.md:5-6` и
  `CONTRIBUTING.md:9,18,39,92` — заменить на `gvoronin7-star`.
- [ ] Задокументировать `USE_EXISTING_INDEX` в `.env.example` (уже
  известная задача №1 из общего `CLAUDE.md`) — вероятно, отдельный
  `OPENAI_API_KEY` не обязателен, `PROXY_API_KEY` уже есть.

### Открытые вопросы (решает владелец проекта)

- [ ] Планировался ли переход GUI на ttkbootstrap/customtkinter, или
  это мёртвый остаток раннего этапа?
- [ ] Нужен ли `data/indices/index.faiss`-двойник настоящей базы
  Squeezer вообще, или его можно удалить?
- [ ] `test_debug_bot.py`/`test_openai_embedding.py` в корне — ручные
  диагностические скрипты или пропущенные из автосборки тесты
  (`pytest.ini` их не подхватывает)?

## Объём следующей итерации

Пока не определён — заполняется, когда за это возьмётся сессия внутри
проекта.
