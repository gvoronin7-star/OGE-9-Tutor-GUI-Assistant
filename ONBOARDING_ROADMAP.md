# ONBOARDING_ROADMAP — OGE-9-Tutor-GUI-Assistant

> Живой файл, часть паттерна «два файла вместо одного»
> (`01_vibe-coding-agent/scenarios/two-track-development.md`), только
> экспортированного сюда, в целевой проект. Обновляется сессиями,
> работающими внутри этого репозитория — не агентом-аудитором. Источник
> текущего состояния — `01_vibe-coding-agent/reports/
> 2026-08-16_OGE-9-Tutor-GUI-Assistant_fix-plan.md` (внешний аудит +
> план исправлений). Рядом — пустая папка `decisions/`: append-only
> журнал решений по этому проекту, тот же формат, что в
> `01_vibe-coding-agent`.

## Текущее состояние

По плану исправлений от 2026-08-16 (HEAD аудита `b62f989`) — подробности
решений в `decisions/decision-log.md`:

### Технические правки

- [x] Устаревшая версия в `.env.example` (`APP_VERSION=2.2.0` → `2.3.0`).
- [x] Плейсхолдер `your-username` в `README.md:5-6` и
  `CONTRIBUTING.md:9,18,39,92` — заменён на `gvoronin7-star`.
- [x] Задокументирован `USE_EXISTING_INDEX` в `.env.example` (закомментированная
  строка с пояснением).
- [x] `pytest.ini` — `testpaths` **временно** был расширен на 5 файлов
  вне `tests/`, затем **откачен обратно к `tests`**: проверка реального
  прогона CI показала, что `test_debug_bot.py` не проходит даже
  `ast.parse` (`IndentationError`) и ломает сбор тестов целиком —
  ни один из 14 иначе собираемых тестов не выполнялся. См. открытый
  вопрос ниже — он же объясняет, почему расширение было ошибкой,
  а не просто более широким охватом.
- [x] `IndentationError` в `test_debug_bot.py:147-149` исправлена
  отдельно (строки внутри `if success:` были на 4 пробела вместо 12) —
  скрипт остаётся вне `testpaths`, но теперь хотя бы запускается вручную
  (`python test_debug_bot.py`).
- [x] CI (оба workflow — `Lint`, `Python Tests`) впервые за всю историю
  проекта прошёл полностью зелёным — `black`/`isort` прогнаны по всему
  репозиторию, добавлен `pyproject.toml` с профилями `isort`/`mypy`,
  устранены 2 реальных бага (`api/text_search.py:137`,
  `tests/test_rag_pipeline.py::test_get_ttl_category_top`). Подробности
  — `decisions/decision-log.md`, запись «CI зелёный впервые в истории
  проекта».
- [x] **Задолженность по типизации (mypy) — аннотационный проход: 155 → 47.**
  Три повторяющихся паттерна (не по файлам, а по образцу) устранены без
  изменения поведения: `self.x = None` без `Optional[T]`, обращение
  к дочерним виджетам через `winfo_children()[N]` (закрыто через
  `typing.cast`), идиома `lambda x=x: ...` (заменена на
  `functools.partial`). По пути найдена и исправлена опечатка
  `Optional[callable]` вместо `typing.Callable`. Подробности, включая
  разбор риска с `current_mode`/`hasattr` в `app.py` — в
  `decisions/decision-log.md`, запись «Аннотационный проход по
  mypy-долгу». `black`/`isort`/`pytest`/CI — зелёные после правки.
- [x] **Задолженность по типизации (mypy) — вторая половина: 47 → 0.**
  `mypy . --ignore-missing-imports` теперь чист по всем 50 файлам
  проекта. По пути найдены и исправлены **4 настоящих бага**, не
  просто нехватка аннотаций:
  1. `rag_manager.py` — два метода `_update_status` с одинаковым
     именем (Python использовал последний, без параметров) —
     функция замены RAG-базы крашилась `TypeError` на первом же шаге
     при любой попытке использования. Метод с сообщением переименован
     в `_set_operation_status`.
  2. Там же — окно подтверждения замены базы использовало
     `icon=tk.WARNING`, которого у `tkinter` не существует
     (`AttributeError`) — крашилось ещё раньше бага №1.
  3. `utils/logger.py` — статистика частоты запросов индексируется
     `int`-хэшами везде в коде, но `json.load()` всегда возвращает
     строковые ключи — после любого перезапуска с сохранённой
     статистикой поиск по хэшу переставал находить совпадения,
     частотное кэширование TTL тихо переставало работать для ранее
     виденных запросов. Загрузка теперь конвертирует ключи обратно
     в `int`.
  4. `api/vector_store.py` — `index_chunks()` не проверял, что
     `self.index` реально создан после `initialize()` (если `faiss`
     не установлен) — упал бы `AttributeError`; добавлена та же
     проверка, что уже была в соседнем `search()`.
  Подробности — `decisions/decision-log.md`, запись «mypy-долг закрыт
  полностью». `black`/`isort`/`mypy`/`pytest`(8/8)/CI — все зелёные.

### Открытые вопросы (решает владелец проекта)

- [x] **Делать ли mypy блокирующим в CI теперь, когда долг закрыт?**
  Решено — да: `|| true` снят с mypy-шага в
  `.github/workflows/lint.yml`. Долг на нуле, будущие правки теперь
  не смогут накапливать новый долг незаметно (в отличие от Squeezer,
  где mypy остаётся информативным).
- [x] Переход GUI на ttkbootstrap — решено и реализовано (не мёртвый
  остаток, а доведённый до конца замысел): `app.py`/`theme.py`
  переписаны на `ttkbootstrap`, `customtkinter` не понадобился.
- [x] `data/indices/index.faiss`-двойник — удалён (был потенциально
  опасен: при подключении дал бы тихо неверные результаты поиска
  из-за несовпадения пространств эмбеддингов).
- [x] `test_debug_bot.py`/`test_openai_embedding.py` в корне — **ручные
  диагностические скрипты**, не тесты: у обоих async `main()` +
  `if __name__ == "__main__":`, рассчитаны на прямой запуск, а не сбор
  pytest'ом. `test_openai_embedding.py` вдобавок требует живого
  `PROXY_API_KEY`. `api/test_generator.py`/
  `gui_debugger/components/user/test_solver.py` — тоже не тесты:
  обычные классы (генератор вопросов, GUI-виджет), совпавшие с
  pytest-паттерном `test_*.py` только по имени файла.

### Аудит от 2026-08-31 (свежий, с нуля — не сверка с прошлым)

Полный отчёт: `decisions/2026-08-31_audit-diff.md`. HEAD на момент
аудита `47be8f9`, 14 коммитов с прошлого аудита (порог — 5/10 дней).
**Все находки закрыты** в двух проходах той же сессии — Б-1 отдельно
(`f03049c`), Б-2 по Б-17 + Д-1 по Д-8 + манифесты одним пакетом
(`c874bf6`). Подробности каждого пункта — в самом отчёте (там же
проставлены отметки «ИСПРАВЛЕНО») и в `decisions/decision-log.md`.
Коротко, что было и как закрыто:

- [x] **17 реальных багов в поведении.** Самое заметное: GUI подвисал
  на каждом LLM-запросе (Б-1, отдельная запись в decision-log);
  вкладка «Кэш» в админке звала несуществующие методы и всегда
  падала (Б-2); `hash()`-ключи кэша/статистики сбрасывались при
  каждом перезапуске процесса (Б-4, добавлен `utils/hashing.py`);
  сохранение настроек в админке было фикцией (Б-5); фолбэк
  Redis→InMemoryCache в `main.py` никогда не срабатывал, а если бы
  сработал — `RAGPipeline` всё равно остался бы подключён к старому
  кэшу (Б-7). Остальные 12 находок (Б-3, Б-6, Б-8—Б-17) — по одному
  фиксу каждая, детали в отчёте.
- [x] **Безопасность.** Секретов/инъекций/path traversal не найдено.
  Единственная находка раздела — сам `scripts/check_secrets.py`
  никогда не проваливался независимо от найденных секретов (Б-6) —
  исправлено, проверено на фейковом ключе AWS (exit 1, CRITICAL).
- [x] **Тесты.** Добавлен `tests/test_bugfixes.py` — регрессионные
  тесты для трёх находок с наибольшим риском тихого возврата
  (`stable_query_hash`, JSON int-ключи `QueryStatsLogger`,
  классификация `check_secrets.py`). Осознанно не покрыты
  Tkinter-зависимые фиксы — CI (ubuntu-latest) без дисплея не может
  создавать реальные виджеты; эти проверены вручную (smoke-тесты,
  не закоммичены) и описаны в decision-log.
- [x] **Документация** (Д-1 по Д-8). Битые ссылки на несуществующий
  репозиторий (`oge-tutor` → `OGE-9-Tutor-GUI-Assistant`), выдуманный
  `docker-compose.yml`, недокументированные `OPENAI_BASE_URL`/
  `OPENAI_API_KEY` на реально используемом пути, `LOG_LEVEL`
  подключен по-настоящему (раньше не читался), версия API в `main.py`
  приведена в соответствие с `.env.example`/README.
- [x] **Манифесты.** 8 неиспользуемых пакетов убраны из
  `requirements.txt` (`pydantic`, `pydantic-settings`, `httpx`,
  `tiktoken`, `markdown`, `beautifulsoup4`, `lxml`,
  `prometheus-client`). `aiogram`/`bot`-пакет решились сами удалением
  `test_debug_bot.py` (Б-16, ссылался на сознательно удалённый по
  CHANGELOG функционал бота).

### Проверка качества контента от 2026-09-01

Живой прогон пользовательского сценария (не аудит кода — реальные
вопросы через `RAGPipeline`/`TestGenerator`, в состоянии «из коробки»
без `.env`). Полный отчёт: `decisions/2026-09-01_content-quality-review.md`.

- [x] **Находка 2 — ИСПРАВЛЕНО (`d93e8a4`).** Демо-ответ на любой
  вопрос по теме был дословно одинаковым («…Экономика изучает
  производство…») независимо от вопроса — keyword-matching в
  `LLMClient._generate_fallback()` шёл по всему промпту (инструкция+
  контекст+вопрос), а не по вопросу ученика. Добавлен параметр
  `query` в `generate()`.
- [x] **Находка 3 — ИСПРАВЛЕНО (`d93e8a4`).** Демо-контент (и ответы,
  и вопросы теста) покрывал только 3 из 6 официальных тем ФИПИ —
  «Политика»/«Социальная сфера»/«Сфера духовной культуры» молча
  подменялись темой «человек и общество». Добавлен полноценный
  контент для всех трёх недостающих тем.
- [x] **Находка 1 — ИСПРАВЛЕНО ПОЛНОСТЬЮ (2026-09-01).** Реальная база
  ФИПИ (`RAG_data_base/vector_db/dataset.json`) содержала чанки-мусор
  (разнесённый по буквам текст с титульной страницы PDF, обрыв слов
  на границе чанкинга, фрагменты кодификатора тем, ошибочно нарезанные
  на бессмысленные заголовки). Полная хронология и обоснование —
  `decisions/decision-log.md` (записи «Находка 1: 7 мусорных чанков
  убраны» и позже) и `Squeezer/decisions/decision-log.md` («Качество
  данных ФИПИ», «Данные ФИПИ, шаги 1-2»):
  - Первый проход (без API): убраны 7 чанков чистого мусора с
    титульной страницы (204 → 197), исправлен баг в Squeezer
    (`normalize_text` портил инициалы людей при разворачивании
    сокращений).
  - Владелец предоставил исходный PDF. Экстрактор переключён на
    `pdfplumber` (заметно чище `pypdf` на этом документе) — второй
    независимый баг в `structure_text()` (перенос строки в PDF
    ошибочно распознавался как новый заголовок) найден и исправлен
    попутно.
  - Владелец разрешил платный вызов API — документ полностью
    перечанкован и переэмбеддён (`text-embedding-3-small`, без
    LLM-обогащения метаданных) — 197 → **157 чанков** (стало меньше,
    т.к. больше не создаются мусорные однословные обрывки).
    Прогнан полный `pytest` (19/19) на новой базе — без регрессий.
- [x] **Находка 4 — ИСПРАВЛЕНО (`dc99359`).**
  `gui_debugger/components/user/test_solver.py` (`_use_demo_test`) —
  фолбэк-фолбэк с бессмысленным содержанием («Вариант А - Неверный»,
  «Термин 1»), срабатывающий при сбое инициализации `TestGenerator`.
  Заменён на 3 реальных, фактчекнутых вопроса по каждой из 6 тем ФИПИ.

### Самопроверка сессии от 2026-09-01

`/code-review` (high effort, 8 углов) по всему диффу сессии
(`b62f989..HEAD`) + ручной разбор диффа Squeezer + факт-чек контента
Фазы 1+2. Подробности — `decisions/decision-log.md`, запись
«Самопроверка сессии: 4 находки, 3 исправлены сразу».

- [x] Краш `async_helper.py` при уничтожении виджета во время фонового
  запроса (`winfo_exists()` guard в `_poll()`).
- [x] Утечка активного режима при возврате к экрану выбора режима
  (`app.py`/`user_mode.py` — разрыв, не покрытый прошлым Б-12).
- [x] Админ-панель «Кэш» (`cache_manager.py`) блокировала GUI через
  `asyncio.run()` — не попала в исходный список Б-1, теперь на общем
  паттерне `run_async_in_background()`.
- [x] Лишний повторный `ping()` Redis при старте (`main.py`).
- [x] Дублирование демо-вопросов в `api/test_generator.py`
  (`_generate_demo_questions()` vs `_get_demo_questions_by_topic()`) —
  ИСПРАВЛЕНО (`c540b75`), сведены к одному источнику
  `_demo_questions_db()`.
- [ ] Осознанно не тронуто: `AsyncHelper.stop()` без вызывающего кода,
  `rag_manager.py`'s свой поток вместо общего `async_helper` — детали
  и обоснование в decision-log.

### UX/UI-аудит мобильного приложения от 2026-09-03

Все 5 фаз концепт-плана (`decisions/
2026-09-01_flutter-mobile-app-concept-plan.md`) реализованы, MVP
работает на реальном устройстве. Полный визуальный и функциональный
аудит всех 10 экранов мобильного приложения — `decisions/
2026-09-03_mobile-ui-ux-audit.md`. **Все 11 находок закрыты** тем же
днём (критичные, важные, желательные) — подробности каждой правки
в `decisions/decision-log.md`, записи от 2026-09-03. Коротко:

- [x] Краш поиска в релизной сборке (R8 обрезал `ai.onnxruntime.**`
  без keep-правил) и нерабочий на реальном телефоне дефолтный адрес
  сервера (`10.0.2.2:8000`, алиас эмулятора) — оба критично.
- [x] Статус «изучено» в списке тем, полужирные термины в статьях,
  иконка (не только цвет) для верно/неверно, разбор ответов + «Пройти
  ещё раз» на экране результата, визуальное отличие «Темы»/«Тесты» —
  все важные.
- [x] Онбординг при первом запуске, версия сборки на экране «Помощь»,
  кольцо прогресса вместо голой дроби, тёмная тема проверена вживую
  (без замечаний) — все желательные.

## Объём следующей итерации

Находка 1 (качество данных базы ФИПИ) закрыта полностью
(2026-09-01, см. выше) — база перечанкована и переэмбеддена с
исправленным пайплайном Squeezer, 197 → 157 чанков. Остальные
находки прошлых проверок (аудит от 2026-08-31, проверка качества и
самопроверка сессии от 2026-09-01, UX/UI-аудит мобильного приложения
от 2026-09-03) закрыты, CI (оба workflow) зелёный, mypy-долг на нуле.
Единственный открытый пункт — качество офлайн-поиска для процедурных
фрагментов длиннее 80 символов (раздел 11 концепт-плана мобильного
приложения) — решение оставлено владельцу, не тронуто.
